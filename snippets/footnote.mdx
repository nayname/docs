export const Footnote = ({ children }) => {
  return (
    <span
      style={{
        fontSize: '0.8em',
        opacity: 0.75,
        display: 'block',
        marginTop: '0.5em',
      }}
    >
      {children}
    </span>
  );
}
