---
title: "Cosmos EVM Quickstart"
description: "Getting started with Cosmos EVM development"
---

# Cosmos EVM Quickstart Guide

This guide provides a foundation for getting started with Cosmos EVM development based on standard Ethereum development practices.

{/*
############################
IMPORTANT: Specific installation procedures, repository locations, 
CLI commands, and network configurations require verification from official sources
############################
*/}

## Prerequisites

Standard development environment requirements:

- Go 1.21+ (for building from source, if needed)
- Node.js (v18 or higher) 
- npm or yarn
- A code editor (VS Code recommended)
- Git

<Note>
Cosmos EVM is built on Cosmos SDK which requires Go, and EVM compatibility suggests Node.js tooling support.
</Note>

## Development Approach

### Smart Contract Development

Since Cosmos EVM aims to be forward-compatible with Ethereum, standard Ethereum development tools should work:

```bash
# Standard Ethereum development dependencies
npm install --save-dev hardhat @nomicfoundation/hardhat-toolbox
npm install --save ethers
npm install --save @cosmjs/stargate @cosmjs/proto-signing
```

### Hardhat Configuration Template

```typescript
import { HardhatUserConfig } from "hardhat/config";
import "@nomicfoundation/hardhat-toolbox";
import * as dotenv from "dotenv";

dotenv.config();

const config: HardhatUserConfig = {
  solidity: {
    version: "0.8.19",
    settings: {
      optimizer: {
        enabled: true,
        runs: 200,
      },
    },
  },
  networks: {
    local: {
      url: "http://localhost:8545", // Standard EVM JSON-RPC port
      accounts: process.env.PRIVATE_KEY ? [process.env.PRIVATE_KEY] : [],
      // ############################
      // # VERIFY: Chain ID
      // ############################
    },
  },
};

export default config;
```

{/*
############################
RESEARCH REQUIRED: 
- Actual network endpoints
- Chain IDs 
- Connection parameters
############################
*/}

## Smart Contract Development

### Basic Contract Example

Standard Solidity contracts should work with Cosmos EVM due to forward compatibility:

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

contract CosmosEVMExample {
    string public message;
    address public owner;
    uint256 public updateCount;
    
    event MessageUpdated(
        string indexed newMessage, 
        address indexed updatedBy, 
        uint256 timestamp
    );
    
    constructor(string memory _message) {
        message = _message;
        owner = msg.sender;
        updateCount = 0;
        emit MessageUpdated(_message, msg.sender, block.timestamp);
    }
    
    modifier onlyOwner() {
        require(msg.sender == owner, "Only owner can call this function");
        _;
    }
    
    function updateMessage(string memory _newMessage) public {
        require(bytes(_newMessage).length > 0, "Message cannot be empty");
        
        message = _newMessage;
        updateCount++;
        
        emit MessageUpdated(_newMessage, msg.sender, block.timestamp);
    }
    
    function updateMessageOwnerOnly(string memory _newMessage) public onlyOwner {
        require(bytes(_newMessage).length > 0, "Message cannot be empty");
        
        message = _newMessage;
        updateCount++;
        
        emit MessageUpdated(_newMessage, msg.sender, block.timestamp);
    }
    
    function getInfo() public view returns (
        string memory currentMessage,
        address contractOwner,
        uint256 totalUpdates,
        uint256 blockNumber,
        uint256 blockTimestamp
    ) {
        return (
            message,
            owner,
            updateCount,
            block.number,
            block.timestamp
        );
    }
}
```

### Deployment Script Template

```typescript
import { ethers } from "hardhat";

async function main() {
  console.log("Deploying to Cosmos EVM...");
  
  // Get deployer account
  const [deployer] = await ethers.getSigners();
  console.log("Deploying with account:", deployer.address);
  
  // Check balance
  const balance = await deployer.getBalance();
  console.log("Account balance:", ethers.utils.formatEther(balance), "tokens");
  
  // Deploy contract
  const CosmosEVMExample = await ethers.getContractFactory("CosmosEVMExample");
  const contract = await CosmosEVMExample.deploy("Hello Cosmos EVM!");
  
  await contract.deployed();
  
  console.log(`✅ Contract deployed to: ${contract.address}`);
  console.log(`📝 Transaction hash: ${contract.deployTransaction.hash}`);
  
  // Test the contract
  const message = await contract.message();
  console.log(`📄 Initial message: "${message}"`);
  
  const info = await contract.getInfo();
  console.log("📊 Contract info:", {
    message: info.currentMessage,
    owner: info.contractOwner,
    updateCount: info.totalUpdates.toString(),
    blockNumber: info.blockNumber.toString(),
  });
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error("❌ Deployment failed:", error);
    process.exit(1);
  });
```

{/*
############################
RESEARCH REQUIRED - Complete sections below

Installation and Setup:
- Official repository URL and location
- Installation procedures (binary releases vs building from source)
- CLI tool names and installation methods
- Local node initialization commands

Network Configuration:
- Available networks (mainnet, testnet, local)
- RPC endpoints and connection details
- Chain ID formats and conventions
- Account setup and key management procedures

Development Tools:
- CLI command syntax and options
- Local development node setup
- Build system integration
- Testing framework specifics

Module Integration:
- x/erc20 module usage for token conversions
- Cosmos SDK integration patterns
- IBC functionality access from smart contracts
- Governance parameter interaction
############################
*/}

## Known Architecture

Based on available information, Cosmos EVM includes:

### Core Modules
- **x/vm Module**: Core EVM implementation (renamed from x/evm)
- **x/feemarket Module**: EIP-1559 fee market mechanism  
- **x/erc20 Module**: Cosmos SDK coin ↔ ERC-20 token interoperability

### Capabilities
- Forward-compatible with Ethereum
- Can run any valid Ethereum smart contract
- Implements features beyond standard EVM
- Integrates with Cosmos SDK for governance and IBC

## Development Workflow Template

Once official documentation is verified, the typical workflow would be:

<Steps>
  <Step title="Environment Setup">
    Install required tools and set up development environment using verified procedures.
  </Step>
  <Step title="Local Node">
    Initialize and start a local Cosmos EVM node for development testing.
  </Step>
  <Step title="Smart Contracts">
    Develop and test smart contracts using standard Ethereum tooling.
  </Step>
  <Step title="Deployment">
    Deploy contracts to local, testnet, or mainnet environments.
  </Step>
  <Step title="Integration">
    Explore Cosmos-specific features like IBC and governance integration.
  </Step>
</Steps>

## Critical Research Areas

{/*
############################
CRITICAL RESEARCH AREAS:

1. Official Documentation: Locate and verify official Cosmos EVM repository and documentation
2. Installation Guide: Confirm installation procedures and CLI tools
3. Network Details: Verify available networks, endpoints, and configuration
4. Examples: Find official examples and tutorials
5. Module Usage: Research how to interact with x/erc20 and other Cosmos-specific features
############################
*/}

## Placeholder Commands

<Note>
**These commands are typical patterns but need verification:**

```bash
# Typical installation pattern (NEEDS VERIFICATION)
go install github.com/cosmos/cosmos-evm/cmd/evmd@latest

# Typical node initialization (NEEDS VERIFICATION) 
evmd init mynode --chain-id cosmos_9000-1

# Typical account creation (NEEDS VERIFICATION)
evmd keys add mykey

# Standard compilation
npx hardhat compile

# Standard deployment (network needs verification)
npx hardhat run scripts/deploy.ts --network local
```
</Note>

## Resources for Research

- **[Architecture Guide](/docs/evm/architecture)** - Technical architecture information
- **[Local Development](/docs/evm/local-development)** - Development setup (needs verification)
- **[Integration Guide](/docs/evm/integrate)** - Chain integration steps