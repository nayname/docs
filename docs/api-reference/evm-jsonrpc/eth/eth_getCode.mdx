---
title: "eth_getCode"
description: "Returns code at a given address"
---

## Overview

Returns code at a given address

### Parameters

1. `address` - Parameter description
2. `block_number` - Parameter description

### Returns

Contract bytecode

## Examples

### Request

<CodeGroup>
```bash cURL
curl -X POST https://evm-rpc.cosmos.network \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "eth_getCode",
    "params": ["0x742d35cc6644c068532fddb11B4C36A58D6D3eAb", "latest"],
    "id": 1
  }'
```

```javascript JavaScript (Web3.js)
const Web3 = require('web3');
const web3 = new Web3('https://evm-rpc.cosmos.network');

// Example call
const result = await web3.eth.getCode(0x742d35cc6644c068532fddb11B4C36A58D6D3eAb, latest);
console.log(result);
```

```javascript JavaScript (Ethers.js)
const { ethers } = require('ethers');
const provider = new ethers.providers.JsonRpcProvider('https://evm-rpc.cosmos.network');

// Example call
const result = await provider.send('eth_getCode', ["0x742d35cc6644c068532fddb11B4C36A58D6D3eAb", "latest"]);
console.log(result);
```

```python Python (Web3.py)
from web3 import Web3

w3 = Web3(Web3.HTTPProvider('https://evm-rpc.cosmos.network'))

// Example call
result = w3.manager.request_blocking('eth_getCode', ['0x742d35cc6644c068532fddb11B4C36A58D6D3eAb', 'latest'])
print(result)
```
</CodeGroup>

### Response

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": "// Method-specific result"
}
```

### Error Response

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32600,
    "message": "Invalid Request"
  }
}
```

## Notes

- This method is compatible with the Ethereum JSON-RPC specification
- Available on both mainnet and testnet endpoints
- Rate limits may apply on public endpoints

## Related Methods

- [`eth_accounts`](./eth_accounts)
- [`eth_getBalance`](./eth_getBalance)
- [`eth_getStorageAt`](./eth_getStorageAt)

## See Also

- [Complete JSON-RPC API Reference](../index)
- [EVM Development Guide](/docs/evm/developers)
- [Network Configuration](/docs/evm/developers/local-setup)
